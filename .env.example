# Conexão Postgres (Vercel Postgres, Neon, Supabase ou local).
# Em Vercel: provisione "Postgres" no projeto, será injetada automaticamente.
DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require

# Para Neon/Vercel Postgres descomente. Para local sem SSL, mantenha 0.
# PGSSL=1
